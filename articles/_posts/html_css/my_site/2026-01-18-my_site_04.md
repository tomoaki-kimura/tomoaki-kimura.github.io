---
layout: post
part: HTML/CSS
title: webサイト
categories: [html_css, my_site]
chapter: 1
section: 4
section_title: レスポンシブ対応をする
description: メディアクエリを使い、スマートフォンでは3枚のパネルが縦並びになるようにします。
---

## {{ page.section_title }}

### スマートフォンでの見え方を確認する

ブラウザの表示幅を狭くしてみましょう。3枚のパネルが横に並んだまま縮んでいき、幅が狭くなるほど写真や文字が窮屈になっていきます。スマートフォンの画面でこのまま表示すると、文字が小さすぎて読みにくくなってしまいます。

### メディアクエリで縦並びに切り替える

CSSチャプターで学んだ`@media`と、Flexboxの`flex-direction`を組み合わせます。

```css
@media (max-width: 600px) {
  .panels {
    flex-direction: column;
    padding: 20px;
  }

  .hero h1 {
    font-size: 1.5rem;
  }
}
```

`flex-direction: column`によって、画面幅が`600px`以下になったときだけ、3枚のパネルが縦に並ぶようになります。あわせて、ヒーローのタイトルも少し小さくして、狭い画面でも収まりが良くなるようにしています。

### 開発者ツールで確認する

ブラウザの開発者ツールには、スマートフォンの画面幅をシミュレートできるモードがあります（Chromeなら`command + shift + M`）。これを使って、`600px`をまたいだ瞬間にレイアウトが切り替わることを確認しましょう。

#### *ここで試したいこと

- `max-width: 600px`の値を`900px`に変えて、レイアウトが切り替わるタイミングがどう変わるか確認してみよう。

### ここまでのコード

index.htmlは前回から変わっていません。

style.css

```css
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  font-family: "Hiragino Sans", "Noto Sans JP", sans-serif;
  line-height: 1.7;
}

.hero {
  position: relative;
  height: 400px;
  background-image: url("images/hero.jpg");
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
}

.hero h1 {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  color: white;
  font-size: 2rem;
  text-align: center;
  text-shadow: 0 2px 8px rgba(0, 0, 0, 0.6);
}

.panels {
  display: flex;
  gap: 24px;
  padding: 40px;
}

.panel {
  flex: 1;
  background-color: #f5f5f0;
  border-radius: 8px;
  overflow: hidden;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

.panel img {
  width: 100%;
  height: 200px;
  object-fit: cover;
}

.panel h3,
.panel p {
  padding: 0 20px;
}

.panel h3 {
  padding-top: 16px;
}

.panel p {
  padding-bottom: 20px;
}

@media (max-width: 600px) {
  .panels {
    flex-direction: column;
    padding: 20px;
  }

  .hero h1 {
    font-size: 1.5rem;
  }
}
```
