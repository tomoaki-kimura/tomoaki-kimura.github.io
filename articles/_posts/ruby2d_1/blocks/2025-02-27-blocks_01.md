---
layout: post
part: Ruby2D(1)
title: ブロック崩し
categories: [ruby2d_1, blocks]
chapter: 1
section: 1
section_title: ファイルの作成
description: ゲーム画面の立ち上げをします。
---

## {{ page.section_title }}

### gem のインストール

基礎編を済ませていると、`ruby2d` はインストールされているはずですが、

もし、開発環境が変わったりしている場合は、再度2D描写用のライブラリをインストールしましょう。

インストールは、ターミナルから以下のコマンドを実行しましょう。

それぞれの環境のホームディレクトリからスタートとします。

```bash
$ gem install ruby2d
```

すると、以下のようなログが流れてインストールが完了します。

```bash
$ gem install ruby2d
Building native extensions. This could take a while...
Successfully installed ruby2d-0.12.1
Parsing documentation for ruby2d-0.12.1
Done installing documentation for ruby2d after 2 seconds
1 gem installed
```

次に、プロジェクトを作成します。ホームディレクトリから、

```bash
$ mkdir ruby2d
$ mkdir ruby2d/blocks
$ cd ruby2d/blocks
```

を実行して、`blocks` ディレクトリに入りましょう。

最後に、以下のコマンドでメインとなるファイルを作っておきましょう。

```
ruby2d/blocks $ touch main.rb 
```

`blocks` 直下に `main.rb` を作成されているはずです。

ここまでで今回の環境が整いましたので、VSCodeを開き直しておくと良いでしょう。

VSCodeが開きなおせたら、コーディングに進みます。

`main.rb` の内容を以下のように書きましょう。

### main.rb の作成

main.rb

```
require 'ruby2d'

bar = Rectangle.new(width: 100, height: 15)
bar.x = (Window.width - bar.width) / 2
bar.y = Window.height - bar.height * 3

show
```

### ファイルの実行

```bash
ruby2d/blocks $ ruby main.rb
```

### 結果

NativeWindowが立ち上がり、自機となるバーが表示されていればOK。

<img src="/assets/images/posts/ruby2d_1/hebi_0001.jpg" alt="立ち上がるウインドウ画像" class="w-full md:w-md">

停止する場合は、立ち上がったウインドウを閉じるか、`ctrl` + `c` で、強制的に止めてしまいましょう。
