---
layout: post
part: Ruby2D(1)
title: 川下りゲーム
categories: [ruby2d_1, river]
chapter: 1
section: 4
section_title: 衝突判定でゲームオーバー
description: contains?を使ってボートと岩の当たり判定を作り、ゲームオーバー画面を表示します。
---

## {{ page.section_title }}

### ゲームの状態を管理する変数を作る

ゲームが「プレイ中」なのか「ゲームオーバー」なのかを、`status` というシンボルの変数で管理します。

main.rb

```ruby
require "ruby2d"

# 川（背景）
river = Rectangle.new(width: Window.width, height: Window.height, color: "blue")

# ボート
boat = Rectangle.new(width: 60, height: 30, color: "brown")
boat.x = (Window.width - boat.width) / 2
boat.y = Window.height - boat.height * 2

# 岩
rock = Circle.new(radius: 15, color: "gray")
rock.x = rand(0..Window.width)
rock.y = 0

status = :playing

# ゲームオーバー画面
message = [
  Text.new("GAME OVER", size: 30, x: 220, y: 200),
  Text.new("press key 'R' to restart", size: 20, x: 190, y: 260)
]
message.each { |text| text.remove }

(略)
```

`Text.new` で作った文字は、`.remove` で一旦画面から消しておきます。あとでゲームオーバーになったときに `.add` で表示します。

### 当たり判定とキー操作を追加する

`boat.contains?(rock.x, rock.y)` で、ボートの中に岩の座標が入っているかどうかを判定できます。

main.rb

```ruby
(略)

on :key_down do |event|
  if status == :playing
    case event.key
    when "left"
      boat.x -= 20 if boat.x > 0
    when "right"
      boat.x += 20 if boat.x < Window.width - boat.width
    end
  end

  if event.key == "r" && status == :game_over
    status = :playing
    boat.x = (Window.width - boat.width) / 2
    rock.y = 0
    rock.x = rand(0..Window.width)
    message.each { |text| text.remove }
  end
end

update do
  if status == :playing
    rock.y += 4
    if rock.y > Window.height
      rock.y = 0
      rock.x = rand(0..Window.width)
    end
    if boat.contains?(rock.x, rock.y)
      status = :game_over
      message.each { |text| text.add }
    end
  end
end

show
```

`status == :playing` のときだけキー操作と岩の移動を受け付けるようにして、ゲームオーバー中は止まって見えるようにしています。`"r"` キーを押すとリスタートします。

### ファイルの実行

```bash
ruby2d/river $ ruby main.rb
```

### 結果

岩がボートに当たると「GAME OVER」の文字が表示され、ボートも岩も動かなくなる。`R` キーを押すと最初からやり直せる。

### ここまでのコード

main.rb

```ruby
require "ruby2d"

# 川（背景）
river = Rectangle.new(width: Window.width, height: Window.height, color: "blue")

# ボート
boat = Rectangle.new(width: 60, height: 30, color: "brown")
boat.x = (Window.width - boat.width) / 2
boat.y = Window.height - boat.height * 2

# 岩
rock = Circle.new(radius: 15, color: "gray")
rock.x = rand(0..Window.width)
rock.y = 0

status = :playing

# ゲームオーバー画面
message = [
  Text.new("GAME OVER", size: 30, x: 220, y: 200),
  Text.new("press key 'R' to restart", size: 20, x: 190, y: 260)
]
message.each { |text| text.remove }

on :key_down do |event|
  if status == :playing
    case event.key
    when "left"
      boat.x -= 20 if boat.x > 0
    when "right"
      boat.x += 20 if boat.x < Window.width - boat.width
    end
  end

  if event.key == "r" && status == :game_over
    status = :playing
    boat.x = (Window.width - boat.width) / 2
    rock.y = 0
    rock.x = rand(0..Window.width)
    message.each { |text| text.remove }
  end
end

update do
  if status == :playing
    rock.y += 4
    if rock.y > Window.height
      rock.y = 0
      rock.x = rand(0..Window.width)
    end
    if boat.contains?(rock.x, rock.y)
      status = :game_over
      message.each { |text| text.add }
    end
  end
end

show
```
