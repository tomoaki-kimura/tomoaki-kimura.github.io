---
layout: post
part: Ruby2D(1)
title: シューティングゲーム
categories: [ruby2d_1, shooting]
chapter: 1
section: 1
section_title: ファイルの作成
description: バンドラーのインストールと、ゲーム画面の立ち上げをします。
---

## {{ page.section_title }}

### bundler のインストール

すでに必要なgemはインストールされていすはずですが、もし、rubyやgemのバージョン違いで、作ったアプリケーションが動かない事があると大変です。

そのため、実際のRubyのプロジェクトには`Gemfile`というgemのリストファイルを用意して、使用しているプログラムのバージョンを指定してインストールする仕組みがあります。

まずは、以下コマンドを実行しましょう。

```bash
$ gem install bundler
```

すると、以下のようなログが流れてインストールが完了します。

```bash
$ gem install ruby2d
Building native extensions. This could take a while...
Successfully installed ruby2d-0.12.1
Parsing documentation for ruby2d-0.12.1
Done installing documentation for ruby2d after 2 seconds
1 gem installed
```

次に、プロジェクトを作成します。ホームディレクトリから、

```bash
$ mkdir ruby2d
$ mkdir ruby2d/hebi
$ cd ruby2d/hebi
```

を実行して、`hebi` ディレクトリに入りましょう。

最後に、以下のコマンドでメインとなるファイルを作っておきましょう。

```
ruby2d/hebi $ touch main.rb 
```

`hebi` 直下に `main.rb` を作成されているはずです。

ここまでで今回の環境が整いましたので、VSCodeを開き直しておくと良いでしょう。

VSCodeが開きなおせたら、コーディングに進みます。

`main.rb` の内容を以下のように書きましょう。

### main.rb の作成

main.rb

```
requie "ruby2d"

show
```

### ファイルの実行

```bash
ruby2d/hebi $ ruby main.rb
```

### 結果

NativeWindowが立ち上がればOK。

<img src="/assets/images/posts/ruby2d_1/hebi_0001.jpg" alt="立ち上がるウインドウ画像" class="w-full md:w-md">

停止する場合は、立ち上がったウインドウを閉じるか、`ctrl` + `c` で、強制的に止めてしまいましょう。
