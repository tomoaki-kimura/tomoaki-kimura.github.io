---
layout: post
part: Ruby2D(2)
title: シューティングゲーム
categories: [ruby2d_2, shooting]
chapter: 1
section: 10
section_title: 背景の星を作る
description: 流れ落ちる星で宇宙の背景を作ります。tickを使った定期処理のパターンを学びます。
---

## {{ page.section_title }}

### Starクラスを作る

`app/models/star.rb` を新規作成しましょう。

app/models/star.rb

```ruby
class Star < Circle
  attr_accessor :speed

  def self.set(stars)
    star = self.new
    star.x = rand(2..Window.width - 2)
    position = rand(0..4)
    star.color = "random"
    if position == 0 || position == 2
      star.speed = 3
      star.radius = 2
      star.color.opacity = 0.8
    else
      star.speed = 2
      star.radius = 1.5
      star.color.opacity = 0.6
    end
    stars << star
  end

  def self.move(stars)
    stars.each do |star|
      star.y += star.speed
      if star.y > Window.height
        star.remove
        stars.delete(star)
      end
    end
  end
end
```

大きい星（速い）と小さい星（遅い）を混在させることで奥行きを演出しています。

### config.rb に require を追加する

config.rb

```ruby
require "ruby2d"
include Math

set width: 600, height: 800

require "./app/messages/game_title"
require "./app/status"
require "./app/models/fighter"
require "./app/models/bullet"
require "./app/models/star"
```

### app/main.rb で星を動かす

app/main.rb（stars を追加・update do を更新）

```ruby
require "./config"

@status = Status.new
fighter = Fighter.new
game_title = GameTitle.new
bullets = []
stars = []
tick = 0

update do
  Star.set(stars) if tick % 10 == 0
  Star.move(stars)

  case @status.game_status
  when :title
  when :start
    fighter.opening_action
    Bullet.move(bullets)
  end
  tick += 1
end
```

`tick % 10 == 0` は「10フレームに1回」星を追加する処理です。

### ファイルの実行

```bash
shooting $ bundle exec rake
```

### 結果

タイトル画面から星が降り注ぐ。ゲーム開始後も降り続ける。

#### *ここで試したいこと

- `tick % 10` の数字を変えて星の出現頻度を変えてみよう（小さくすると多く、大きくすると少なくなる）。
- `star.color.opacity` を変えて星の透明度を調整してみよう。

### ここまでのコード

app/models/star.rb

```ruby
class Star < Circle
  attr_accessor :speed

  def self.set(stars)
    star = self.new
    star.x = rand(2..Window.width - 2)
    position = rand(0..4)
    star.color = "random"
    if position == 0 || position == 2
      star.speed = 3
      star.radius = 2
      star.color.opacity = 0.8
    else
      star.speed = 2
      star.radius = 1.5
      star.color.opacity = 0.6
    end
    stars << star
  end

  def self.move(stars)
    stars.each do |star|
      star.y += star.speed
      if star.y > Window.height
        star.remove
        stars.delete(star)
      end
    end
  end
end
```

app/main.rb

```ruby
require "./config"

@status = Status.new
fighter = Fighter.new
game_title = GameTitle.new
bullets = []
stars = []
tick = 0

update do
  Star.set(stars) if tick % 10 == 0
  Star.move(stars)

  case @status.game_status
  when :title
  when :start
    fighter.opening_action
    Bullet.move(bullets)
  end
  tick += 1
end

on :key do |event|
  key = event.key
  case fighter.status
  when :ready
    fighter.move(key)
  end
end

on :key_down do |event|
  key = event.key
  case key
  when "space"
    if @status.game_status == :title
      game_title.remove
      fighter.status = :clear
      @status.to_start
      fighter.add
    end
  when "j"
    if fighter.status == :ready
      Bullet.set(bullets, fighter)
    end
  end
end

on :key_up do |event|
  fighter.nutral(event.key)
end

show
```
