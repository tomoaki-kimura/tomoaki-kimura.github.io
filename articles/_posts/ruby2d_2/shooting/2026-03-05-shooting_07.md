---
layout: post
part: Ruby2D(2)
title: シューティングゲーム
categories: [ruby2d_2, shooting]
chapter: 1
section: 7
section_title: Bulletクラスを作る
description: 弾のクラスを作り、Jキーで発射できるようにします。クラスメソッド（self.）の使い方を学びます。
---

## {{ page.section_title }}

### Bulletクラスを作る

弾は `Circle` を継承して作ります。`app/models/bullet.rb` を新規作成しましょう。

app/models/bullet.rb

```ruby
class Bullet < Circle
  attr_accessor :speed, :angle

  def initialize
    super(radius: 3)
    self.speed = 15
    self.angle = 0
  end

  def self.set(bullets, fighter)
    bullet = self.new
    bullet.x = fighter.x + (fighter.width / 2)
    bullet.y = fighter.y
    bullets << bullet
  end
end
```

`self.set` は `Bullet.set(...)` のように呼び出すクラスメソッドです。`bullets` 配列に弾を追加します。

### config.rb を更新する

config.rb

```ruby
require "ruby2d"

set width: 600, height: 800

require "./app/messages/game_title"
require "./app/status"
require "./app/models/fighter"
require "./app/models/bullet"
```

### app/main.rb に発射処理を追加する

app/main.rb

```ruby
require "./config"

@status = Status.new
fighter = Fighter.new
game_title = GameTitle.new
bullets = []

update do
  case @status.game_status
  when :title
  when :start
    fighter.opening_action
  end
end

on :key do |event|
  key = event.key
  case fighter.status
  when :ready
    fighter.move(key)
  end
end

on :key_down do |event|
  key = event.key
  case key
  when "space"
    if @status.game_status == :title
      game_title.remove
      fighter.status = :clear
      @status.to_start
      fighter.add
    end
  when "j"
    if fighter.status == :ready
      Bullet.set(bullets, fighter)
    end
  end
end

on :key_up do |event|
  fighter.nutral(event.key)
end

show
```

### ファイルの実行

```bash
shooting $ bundle exec rake
```

### 結果

Jキーを押すと自機の先端に白い丸が生成される。まだ動かないので重なって見える。

#### *ここで試したいこと

- `radius` を変えて弾のサイズを変えてみよう。
- 連打して弾がどんどん重なることを確認しよう。

### ここまでのコード

app/models/bullet.rb

```ruby
class Bullet < Circle
  attr_accessor :speed, :angle

  def initialize
    super(radius: 3)
    self.speed = 15
    self.angle = 0
  end

  def self.set(bullets, fighter)
    bullet = self.new
    bullet.x = fighter.x + (fighter.width / 2)
    bullet.y = fighter.y
    bullets << bullet
  end
end
```
